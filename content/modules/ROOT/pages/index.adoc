= AI-Powered Developer Platform on ROSA

Welcome to the *AI-Powered Developer Platform on ROSA* workshop.

The story for today is a Monday at a company that will not send source code to a public LLM. Security approved an *in-cluster coding agent*: Qwen3.8 on a GPU, OpenCode in the browser, no Copilot, no `api.openai.com`.

You wear two hats. In the morning you are the platform engineer (Alex): GPU, operators, Models-as-a-Service, one API key. In the afternoon you are the new hire (Sam): open a Dev Space, prompt Fortune Cookie into production, then audit it. Before you leave, you look at the GPU and token bill together.

== Who This Workshop Is For

[cols="1,1", options="header"]
|===
| Persona | Role in This Workshop

| *Platform Engineer* (morning)
| Provisions GPU capacity, installs operators, publishes Qwen3.8 behind a governed OpenAI-compatible API, and hands developers a short `AGENTS.md` (~45 lines) — not a runbook wiki.

| *Developer* (afternoon)
| Launches OpenShift Dev Spaces, talks to OpenCode backed by that in-cluster LLM, ships a real app through Tekton and Argo CD, then asks the same model to audit the manifests.
|===

== What You Will Build

image::architecture-overview.png[In-cluster coding agent on ROSA, align="center"]

The LLM writes the app. The Fortune Cookie app does *not* call the LLM — it serves hard-coded fortunes. Qwen3.8 is a *platform* capability.

=== Act 1 — Platform
* A fixed-size *GPU machine pool* on ROSA — one NVIDIA L40S, no manual EC2
* *OpenShift AI* Models-as-a-Service serving *Qwen3.8-27B* (Red Hat AI INT4) behind one HTTPS front door and one `sk-oai-` key
* *OpenShift Dev Spaces* pointed at that gateway
* *AGENTS.md* — the only runbook OpenCode reads
* *OpenShift GitOps* (Argo CD) and *OpenShift Pipelines* (Tekton)

=== Act 2 — Developer
* A personal Git repository via the in-cluster Git server
* A *Fortune Cookie* Go app generated live by OpenCode + Qwen3.8
* Image build and GitOps deploy from `git` / `gitpop` / `oc` — no Ansible, no local Docker
* Platform rules (limits, probes, SCC, TLS) audited by the same model

=== Act 3 — The bill
* NVIDIA DCGM GPU metrics and vLLM token metrics in the same console
* MaaS subscription quota — how the platform team caps cost

== Learning Outcomes

After completing this workshop, you will understand:

* How ROSA managed machine pools give you GPU capacity without EC2 complexity
* How OpenShift AI (`LLMInferenceService` + Models-as-a-Service) is a governed OpenAI-compatible API on your cluster
* How `AGENTS.md` shifts platform governance from documentation to something the model enforces
* How Dev Spaces + OpenCode is an AI-augmented inner loop with no laptop install
* How Tekton and Argo CD form a GitOps path from a single repository
* How GPU watts and token windows are the real cost of an in-cluster coding agent

== Workshop Structure

[cols="1,1,3,1", options="header"]
|===
| Act | Lab | Topic | Duration

| *0 — Get in*
| —
| Access your ROSA cluster, set up CLI credentials
| 20 min

.4+| *1 — Stand up the agent*
| 1.1
| Deploy a GPU Machine Pool
| 15 min (5 active)

| 1.2
| Install Platform Operators
| 5 min active / ~20 min background

| 1.3
| Publish Qwen3.8 as a Service
| 15 min active / model loads in background

| 1.4
| Hand the Keys (`AGENTS.md` + Dev Spaces URL)
| 10 min

.3+| *2 — First day*
| 2.1
| Launch Your Dev Space
| 10 min

| 2.2
| Prompt to Production (generate, ship, change)
| 35 min

| 2.3
| Audit with OpenCode
| 10 min

| *3 — The bill*
| 3.1
| GPU and Token Metrics
| 15 min

| *—*
| —
| Conclusion & Q&A
| 10 min
|===

[TIP]
====
*Fire-and-forget timing:* GPU node, operators, and model load are triggered early and run in the background. By the time Sam opens Dev Spaces, Qwen3.8 is Ready.
====

== Prerequisites

You will need:

* A web browser
* Access to the lab environment (credentials provided by your instructor)
* A terminal — provided inside your Dev Space (no local install required)

Everything else — `rosa`, `oc`, `kubectl`, the OpenCode CLI, the Git server client — is either pre-installed in the lab environment or installed during the workshop steps.

Let's get started.
